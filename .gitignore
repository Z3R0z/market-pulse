# dependencies (Render installs these via `npm install`)
node_modules/

# secrets — never commit your Finnhub key
.env
.env.local
.env.*.local

# logs / OS cruft
npm-debug.log*
*.log
.DS_Store
Thumbs.db
